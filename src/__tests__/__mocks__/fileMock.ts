/**
 * Mock for static file imports (images, SVGs, etc.).
 * Returns a simple string so the import resolves without error in Jest.
 */
const fileMock = 'test-file-stub'
export default fileMock
module.exports = fileMock
