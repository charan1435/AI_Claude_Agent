# Routes the capture hook should screenshot for this project.
# Format: one path per line, or "path:name" for custom output names.
# The capture hook reads this when present, otherwise it falls back to generic defaults.

/:home
/login
/signup
